import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.alfonsohh.financemanager',
    appName: 'financeManager',
    webDir: 'dist/financeManager',
    bundledWebRuntime: false,
};

export default config;
